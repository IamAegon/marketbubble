// profileUrl/channelUrl now live in @app/shared so the server reaction fold and the
// web UI build the same links. Re-exported here so existing imports keep working.
export { profileUrl, channelUrl } from "@app/shared";
