MARKET BUBBLE — BUILD STATUS
============================
(updated 2026-06-07)

================================================================
DONE & VERIFIED
================================================================

UI / POLISH
- Warm "broadcast desk" theme; responsive top-bar (no cramming on resize); Markets view responsive.
- Rooms: creator-named DM labels ("Ansem DM" / "Faze DM"); shared roomIcon/roomTabLabel helper.
- Capitalized "Welcome" greeting (brief modal, toast, settings preview).
- Notifications: close (x) on toasts + per-item x in the right-rail log.
- Portfolio Performance report: thicker bars + wider chart/stat gutter.
- Live theater: collapsible stream-switcher groups (chevron + live count, persisted).

NAV / STRUCTURE
- Analytics section regrouped: NOW (Pulse, Reactions) / REVIEW (Sessions, Compare).
- Recording folded into Pulse as a control strip (standalone Recording page removed).
- Sessions = front door -> per-session "Session Report" drill-in (KPIs + charts + PDF).
- StatsMini rail -> "Full analytics" deep-link to Pulse.
- Transcript moved out of "Studio" into the live cockpit (Live . Transcript . Rooms).

TWITCH ACCOUNT CONNECTIONS (post + moderate, as your own account)
- OAuth connect/disconnect; tokens stored server-side, never sent to the browser.
- Post + reply to a connected Twitch chat as the linked user.
- Moderation (mods only, re-verified by Twitch): timeout / ban / unban / delete / chat-modes / clear.
- Inline mod buttons + reply on Twitch messages; multi-destination "Send to" composer
  (pick MB rooms + Twitch streams, multi-select, one message fans out to all).
- Admin app setup in Settings->Connections: paste Client ID/Secret (no .env/restart),
  editable + HTTPS-validated OAuth redirect URL.
- "From your Twitch follows" picker (Settings->Streams) -> 1-click add (needs user:read:follows; reconnect once).
- Streams list cleanup: tracked-X NEWS accounts no longer show as "streams".
- Emoji picker in the composer (MB + Twitch + Kick); line-icon, right side, inserts at cursor.
- MB send fix: sending auto-scrolls + flashes the destination column in Columns view.

LIVE ANALYTICS ENRICHMENTS (Pulse)
- Hype score (0-100) + acceleration + spike markers on the activity chart.
- New vs returning chatters + return rate + active-uniques/min.
- Per-channel ("by stream") sentiment.
- Most-reacted messages; rising emotes/cashtags (momentum).
- Live Twitch viewer counts (app-token Helix poller -> SideBus -> headline card + per-channel badges).
  Verified with real data (jynxzi ~60k, eslcs ~58k).

PERSISTENCE (code complete + typechecks; no-DB path verified)
- Postgres durable chat + search via DATABASE_URL (PgChatStore; schema auto-creates).
- Boot-rebuild: on start, replays last 6h from the store into the RingBuffer (so refresh
  shows history) AND the StatsAggregator (so live analytics survive a restart).

================================================================
TO DO / PENDING
================================================================

1. VERIFY DURABLE STORAGE (blocked: Docker not running here)
   - `docker compose up -d`  (Postgres :5432)
   - add to packages/server/.env:  DATABASE_URL=postgres://postgres:postgres@localhost:5432/marketbubble
   - restart server -> expect logs "durable history on" then "boot-rebuild replayed N"
   - test: chat a bit -> restart -> analytics + chat backfill should persist.

2. TRENDS -> social/news hot topics (IN PROGRESS)
   - Remove coins/fear&greed. Make it "what people are talking about" across the web/social.
   - Sources (free, no key): Google Trends daily searches, Mastodon trends (tags+links), Reddit hot, Google News.
   - X/Twitter trends: no free official API -> needs a paid API or a fragile 3rd-party scrape (TBD).
   - Instagram / TikTok trends: no public free API; reliable sourcing not feasible without paid/scraping (flagged).

3. GIF picker for MB chats (Tenor) — needs a TENOR_API_KEY (free); degrades gracefully without it.

4. KICK parity — connect/post/moderate on Kick (OAuth 2.1 + PKCE) + Kick streams in the composer.

5. Twitch EventSub (live subs / cheers / raids) — optional, bigger build.

6. COMMIT — large uncommitted stack on branch feat/desk-polish (only the first polish batch is committed: b3f70ce).

================================================================
SETUP NOTES
================================================================
- Twitch app: configured via Settings->Connections (Client ID/Secret stored in packages/server/data/integrations.json).
- Postgres: not yet enabled (DATABASE_URL unset -> in-memory ring; history lost on restart until enabled).
- Tenor (GIFs): not set.
- Server runs from packages/server (dotenv loads packages/server/.env).
